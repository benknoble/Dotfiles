#! /usr/bin/env bash
# a script for running games when you need them

# turn on "strict mode"
set -euo pipefail

bin_dir="$( cd "$( dirname "${BASH_SOURCE[0]}")" && pwd )"

# structure :
#   | name=game_binary
#   | name='game command'
declare -A games

main() {
  :
}

main

# log, die
