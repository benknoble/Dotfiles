# vim: noet:sw=8:sts=8
[alias]
	amend = commit --amend
	b = branch
	bd = branch --delete
	bdf = branch --delete --force
	ca = commit --all
	cc = commit
	ce = config --edit
	cm = commit -m
	co = checkout
	conflict = log --oneline --left-right --merge
	ctags = !.git/hooks/ctags
	d = diff
	g = !exec git
	git = !exec git
	grall = !git graph --all
	graph = !git graph-long --oneline
	graph-long = log --graph
	ig = check-ignore
	igv = !git ig -v
	intend = add --intent-to-add
	last = log -1 HEAD
	logp = log --graph --abbrev-commit --pretty=mine
	logpall = !git logp --all
	lol = log --oneline
	merc = merge --no-ff
	name-when-merged = "!f() { git when-merged -c \"$@\" | git name-rev --stdin;}; f"
	newb = checkout -b
	patch = add --patch
	riu = rebase -i @{u}
	s = status -sb
	sb = show-branch
	sbu = !git sb @ @{u}
	sdiff = !git diff && git submodule foreach 'git diff'
	spush = push --recurse-submodules=on-demand
	standup = !git log --author=$USER --since=yesterday --all --format=%s
	stat = status
	supdate = submodule update --remote --checkout
	unstage = reset HEAD --
	visual = !gitk --all &
[merge]
	tool = fugitive
	conflictstyle = diff3
	log = true
[core]
	excludesfile = ~/.gitignore_global
	pager = less -FRX
[diff]
	submodule = log
	tool = vimdiff
	colorMoved = default
[status]
	submodulesummary = 1
	showstash = true
[push]
	recurseSubmodules = check
[include]
	path = ~/.gitconfig.local
[difftool]
	prompt = no
[mergetool]
	prompt = no
	keepBackup = false
[gitsh "color"]
	default = cyan
[gitsh]
	prompt = "%D (%c%b %#%w) %G>"
	noGreeting = true
	defaultCommand = status -sb && :echo
[mergetool "fugitive"]
	cmd = vim --nofork -c \"Gvdiffsplit!\" \"$MERGED\"
	trustExitCode = true
[fetch]
	recurseSubmodules = on-demand
	prune = true
[rerere]
	enabled = true
[rebase]
	autoSquash = true
[init]
	templateDir = ~/.git_template
[log]
	date = auto:human
[advice]
	waitingForEditor = false
[pretty]
	mine = %C(yellow)%h%Creset%C(bold green)%d%Creset %s %C(magenta)(%cr)%Creset %C(ul bold cyan)<%an>%Creset
