###
# delim : %%
# help : git external command in bash
# output : git-{fname}
# vim: ft=bash
###
#! /usr/bin/env bash

set -euo pipefail

USAGE='usage'
SUBDIRECTORY_OK=true

# source git-sh-setup for some helpers
set +u
source "$(git --exec-path)"/git-sh-setup
set -u

log() {
  printf '%s\n' "$@"
} >&2

main() {
  :
}

main "$@"
